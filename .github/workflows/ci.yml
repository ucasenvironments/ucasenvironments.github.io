name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create file
        run: echo Hello, world! > file.txt
      - name: Git
        run: |
          git add .
          git commit -m "Latest file"
          git push
